import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function Transactions() {
  return (
    <PlaceholderScreen
      title="Transactions"
      phaseNote="Search, category/income filters, and the edit/void row actions — built in Phase 1 once transactions are actually being written by the Home/mobile entry form."
    />
  );
}
