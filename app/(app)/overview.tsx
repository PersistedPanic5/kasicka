import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function Overview() {
  return (
    <PlaceholderScreen
      title="Overview"
      phaseNote="Month switcher, income/spent/net cards, and budget-vs-actual bars per category — built out in the merged Phase 1 alongside the core expense loop, once monthly_budgets has real data to show."
    />
  );
}
