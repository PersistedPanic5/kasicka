import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function Debts() {
  return (
    <PlaceholderScreen
      title="Debts"
      phaseNote="Filters, status badges, share-link + QR reveal, and confirm-settled — the headline feature, built in Phase 1 right alongside core expense entry so it lands as early as possible."
    />
  );
}
